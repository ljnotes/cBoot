#include <stdio.h>

int main()
{
    printf("I am a string, followed by a new line\nAnd this is the second line\n");
    // Other escape characters include
    // \n - New Line (to simulate the enter key)
    // \t - tab
    // \' - Single quote
    // \" - Double quote

    return(0);
}
